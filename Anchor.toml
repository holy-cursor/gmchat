[features]
seeds = false
skip-lint = false

[programs.devnet]
solana_messaging = "YourProgramIdHere"

[programs.mainnet]
solana_messaging = "YourProgramIdHere"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[test]
startup_wait = 5000
